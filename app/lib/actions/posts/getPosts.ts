//..'use server'

//..export default async function getPosts(page : string){


//..}
  //..const postsResponse = await fetch(`${process.env.NEXT_PUBLIC_URL}/api/posts/user`, {
    //..method: "GET",
    //..headers : {"username" : params.username},
    //..cache: "no-store",
  //..});

  //..//const { data : posts } = await postsResponse.json();